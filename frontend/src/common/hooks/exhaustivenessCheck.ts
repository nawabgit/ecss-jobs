/**
 * The function confirmes we've reached a dead-code branch. Useful for making sure we've
 * covered all possible cases and code paths
 * @param t value to test for neverness
 */
export default (t: never) => undefined;
